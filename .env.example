# Local development environment variables

# Backend API token for authenticated requests
API_TOKEN=fortress-hub-api-token

# Frontend environment variable used by Vite for API authorization
VITE_API_TOKEN=fortress-hub-api-token

# Backend server port
PORT=4000
