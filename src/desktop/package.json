{
  "name": "@fortress/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo \"Desktop scaffold ready\"",
    "build": "tsc -p tsconfig.json",
    "build:desktop": "npm run build",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "npm run build && node test/smoke.js",
    "test:desktop": "npm run test"
  },
  "devDependencies": {
    "typescript": "5.3.3",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0"
  }
}
