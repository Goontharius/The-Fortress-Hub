version: '3.9'
services:
  backend:
    image: node:18
    working_dir: /workspace
    volumes:
      - ../../src/backend:/workspace
    command: bash -lc "npm install && npm run dev"
    ports:
      - '4000:4000'
